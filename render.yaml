# Render 一鍵部署用（免費方案）
# 使用方式：把「新聞蒐集」資料夾推上 GitHub，到 render.com 連線該 repo，選 Blueprint，會自動用此檔
services:
  - type: web
    name: news-aggregator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
